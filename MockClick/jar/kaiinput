# Script to start "input" on the device, which has a very rudimentary
# shell.
#
base=/data/local/tmp
export CLASSPATH=$base/kaiinput.jar
exec app_process $base/ com.njnu.kai.mockclick.util.KaiInput "$@"

